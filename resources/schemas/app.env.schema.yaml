type: object
properties:
  env:
    type: object
    properties:
      # APP_SERVICE_core: { $ref: service }
      # APP_SERVICE_datasets: { $ref: service }
      APP_LISTEN_IP_FAMILY: { enum: ["0", "4", "6"], default: "4" }
    required: []
required: [env]
additionalProperties: false
