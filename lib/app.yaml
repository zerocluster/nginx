dependencies:
  - "@softvisio/core"

services:
  server:
    components:
      nginx: true
      storage: true
      rpc: true

defaultService: server

components:
  nginx:
    dockerEnabled: true

    httpSetRealIpFrom: [local, cloudflate, google-cloud-load-balancers]

    streamSetRealIpFrom: [local, cloudflate, google-cloud-load-balancers]

config: ~
